info:
  name: fetch list
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:5000/api/games/list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
