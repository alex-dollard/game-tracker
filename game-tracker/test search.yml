info:
  name: test search
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5000/api/games/search?q=halo
  params:
    - name: q
      value: halo
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
